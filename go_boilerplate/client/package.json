{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build:watch": "vite build --watch --emptyOutDir",
    "build": "vite build --emptyOutDir",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "prettier": "prettier --write src/",
    "generate-assets": "svgo public/icon.svg -o public/icon-optimized.svg && pwa-assets-generator --config pwa-assets.config.ts",
    "bench": "TIMESTAMP=$(date +%Y%m%d_%H%M%S) && npx lighthouse http://localhost:8788 --chrome-flags='--headless --no-sandbox' --only-categories=performance,accessibility,best-practices,seo --output=json,html --output-path=./benchmarks/lighthouse/$TIMESTAMP && npm run bench:format && npm run bench:html",
    "bench:format": "zsh -c 'LATEST=$(ls benchmarks/lighthouse/ | grep .json | tail -1) && HTML=$(ls benchmarks/lighthouse/ | grep .html | head -1) && JSON_FILE=benchmarks/lighthouse/$LATEST && echo \"✅ Results saved to benchmarks/lighthouse\" && echo \"📊 Performance: $(jq .categories.performance.score $JSON_FILE)\" && echo \"♿ Accessibility: $(jq .categories.accessibility.score $JSON_FILE)\" && echo \"🔧 Best Practices: $(jq \".categories[\\\"best-practices\\\"].score\" $JSON_FILE)\" && echo \"🔍 SEO: $(jq .categories.seo.score $JSON_FILE)\" && echo \"📁 View HTML: chromium benchmarks/lighthouse/$HTML\"'",
    "bench:html": "zsh -c 'LATEST=$(ls benchmarks/lighthouse/ | grep .html | tail -1) && chromium benchmarks/lighthouse/$LATEST'"
  },
  "author": "Francesco99975",
  "license": "ISC",
  "dependencies": {
    "alpinejs": "^3.14.9",
    "htmx.org": "^2.0.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@types/alpinejs": "^3.13.11",
    "@types/node": "^24.8.1",
    "@vite-pwa/assets-generator": "^1.0.2",
    "fast-glob": "^3.3.3",
    "lightningcss": "^1.30.2",
    "svgo": "^4.0.0",
    "tailwindcss": "^4.1.5",
    "typescript": "^5.3.2",
    "vite": "7.1.10",
    "vite-plugin-manifest-sri": "^0.2.0",
    "vite-plugin-pwa": "^1.1.0"
  }
}
